# Money Transfer System

This project contains a Spring Boot backend and an Angular frontend for a money transfer system.

## Setup
1. Start MySQL and create the database using `sql/schema.sql`.
2. Run backend with `mvn spring-boot:run` from the `backend` folder.
3. Run frontend with `ng serve` from the `frontend` folder.
